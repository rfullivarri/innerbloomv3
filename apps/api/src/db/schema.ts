/**
 * The project is starting from a clean slate, so no relational tables are
 * currently defined in the application schema. This module intentionally
 * re-exports nothing while keeping the Drizzle integration happy.
 */

export {};
