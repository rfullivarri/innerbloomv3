# Innerbloom supplied-asset campaign v1

Use the strategy snapshot in the copied Admin task as the strategy source. Inspect every selected Drive image before mapping it. Preserve the visual base: no crop, repaint, generation or UI modification. Produce clear short on-image copy with a hook and a CTA; reserve top and bottom Story safe zones. The campaign JSON must satisfy `supplied-asset-campaign-v1.schema.json`, including a valid `campaign.period_key` and one allowed `campaign.objective`. A PR containing that file is a preview handoff only. R2 upload happens after the imported posts are approved in Admin.
